# Terminal UI app

## Purpose

Interactive terminal UI built with OpenTUI and React.

## Read First

- `src/index.tsx` — renderer bootstrap (`createCliRenderer` + `createRoot`)
- `src/app.tsx` — root component and keyboard navigation

## Owns

- Terminal rendering and layout
- Keyboard handling and screen flow
- User-facing TUI screens under `src/`
