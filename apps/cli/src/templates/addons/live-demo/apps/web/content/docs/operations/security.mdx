---
title: Security
description: Validation, secrets, and defaults in generated TypeScript projects.
---

Security here means **baseline hygiene** the scaffold encodes—not a certification.

<Mermaid
  chart={`flowchart TD
  Req["Request"]
  Zod["Zod validation"]
  Policy["policy.ts"]
  Data["service / repository"]
  Req --> Zod --> Policy --> Data
`}
/>

## Input and API

- tRPC procedures use **Zod** (or shared validators) at boundaries
- Prefer policy checks for authorization—not “security by obscurity” routes
- Keep server-only code out of client bundles via package exports

## Secrets and env

- `.env.example` lists required keys; `serverEnv` validates at boot in `packages/backend-common`
- Never commit `.env` with production secrets
- Rotate `BETTER_AUTH_SECRET` if leaked

## Transport

- Production: HTTPS everywhere; align `FRONTEND_URL` and `BETTER_AUTH_URL` with real origins
- CORS is configured on the API for the web origin—update when adding staging domains

## Dependencies

- Run your package manager’s audit in CI (`bun run ci` includes lint/typecheck; add audit if your team requires it)
- Pin catalogs via workspace catalog files when using Bun/pnpm

## Agents

Generated repos include agent maps—not extra attack surface, but **do not paste production secrets** into agent chats. Use `.env` locally and platform secret stores in deploy hosts.

## Reporting

This is a personal/template project. For production apps you ship, follow your own disclosure process.

## Demo endpoints (`/live`)

The live sandbox (proof run, chat, posts) is **intentionally public** for evaluation. Treat it as demo-only.

| Procedure               | Auth | Public data                                        | Notes                                 |
| ----------------------- | ---- | -------------------------------------------------- | ------------------------------------- |
| `hello`                 | No   | Greeting string                                    | Harmless contract check               |
| `post.list`             | No   | Published posts only                               | Drafts filtered server-side           |
| `post.create`           | Yes  | —                                                  | Draft only; max title/content lengths |
| `chat.list`             | No   | Messages + redacted sender (`id`, `name`, `image`) | No email in JSON                      |
| `chat.stats`            | No   | Message count + latest timestamp                   | Activity strip metadata               |
| `chat.send`             | Yes  | —                                                  | Max 280 chars; DB rate limits; visible to all readers |
| `chat.verifySend`       | Yes  | —                                                  | Proof-run / Stack Lab contract check only           |
| `game.leaderboard`      | No   | Top-10 best scores per user                        | Public read                                           |
| `game.myBest`           | Yes  | Personal best score                                | Requires session                                      |
| `game.submitScore`      | Yes  | —                                                  | Cooldown + monotonic best; demo anti-cheat          |
| `game.verifyScore`      | Yes  | —                                                  | Auth + bounds check; no DB write                      |
| `demo.capabilities`     | No   | Demo sync hints (`sse` / `poll`)                   | Build/runtime policy                                  |
| `demo.stackSnapshot`    | No   | SSR snapshot (health, feeds, redis flag)           | No secrets; used by `/live` RSC shell                 |
| `GET /api/live/stream`  | No   | SSE `chat:message`, `game:leaderboard` events      | Heartbeat every 30s; rate limited                     |
| `GET /api/chat/stream`  | No   | Legacy SSE `message` events                        | Prefer `/api/live/stream`                             |
| `auth.getSession`       | No   | Session user or null                               | No secrets                            |
| `auth.getSecretMessage` | Yes  | Protected string                                   | Proof-run only                        |
| `user.findUserByEmail`  | Yes  | Self email only                                    | Returns public user shape             |

**SSE:** One-way push from the API after `chat.send` or `game.submitScore`. Clients append chat messages or invalidate leaderboard queries; polling is the fallback when the stream drops (e.g. cross-origin Vercel web + Render API).

**Before production:** remove `/live`, demo auth pages, and seed chat messages—or restrict these procedures behind admin policy. Run `template-cleanup --remove=live` on generated projects.
