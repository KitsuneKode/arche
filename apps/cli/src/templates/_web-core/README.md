# Web core (author-time source only)

Canonical Next.js web foundation synced into family templates by `toolings/scripts/sync-web-core.ts`.

**Not a scaffold family** — never copied to generated projects directly.
