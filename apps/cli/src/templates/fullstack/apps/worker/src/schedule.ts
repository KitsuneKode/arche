/** Repeatable jobs — add schedules when background work is needed. */
export async function registerSchedules(): Promise<void> {
  // No default schedules. Add repeatable BullMQ jobs here when needed.
}
