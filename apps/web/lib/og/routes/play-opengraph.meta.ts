import { ogImageContentType, ogImageSize } from '@/lib/og/constants'

export const alt = 'Arche Relay — live chat and stack ping'
export const size = ogImageSize
export const contentType = ogImageContentType
